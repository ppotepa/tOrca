# Protocol tests

The v1 contract tests cover identity derivation, signature verification,
challenge mutation, strict onion validation and opaque envelope constraints.
Test fixtures contain public/synthetic data only.
