# Versioning

The initial wire version is `1`. Unknown versions are rejected. Additive
fields require a documented compatibility rule; cryptographic or routing
changes require a new protocol version.
