# `packages/torchat-storage/`

Durable local SQLCipher storage: migrations, SQL catalog, repositories,
transactions, projections, outbox records, and runtime storage integration.
The package has no dependency on the client engine, relay, or platform APIs.
