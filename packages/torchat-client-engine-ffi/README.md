# `packages/torchat-client-engine-ffi/`

The single native C ABI boundary for the client engine. Platform runners may
depend on this package; the FFI layer does not contain UI, storage, or relay
service logic.
