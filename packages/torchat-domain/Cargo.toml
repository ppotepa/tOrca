[package]
name = "torchat-domain"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Runtime-independent TorChat domain vocabulary"

[dependencies]
serde = { version = "1", features = ["derive"] }
