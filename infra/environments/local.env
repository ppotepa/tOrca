# Public local-development manifest. Runtime values live in .torchat/runtime
# and are deliberately not committed.
TORCHAT_ENVIRONMENT=local
TORCHAT_COMPOSE_PROJECT=torchat-local
TORCHAT_HTTP_PORT=28080
TORCHAT_SOCKS_PORT=29050
TORCHAT_FIXTURES=false
