# Shared desktop/client reference configuration.
# Android uses the equivalent bundled template in mobile assets.
ClientOnly 1
SocksPort 127.0.0.1:<SOCKS_PORT>
DataDirectory <DATA_DIRECTORY>
# Preserve the private Tor cache in the client data directory for warm starts.
Log notice stdout
