# Applications

Platform runners only. `mobile/flutter` owns the Android runner and mobile
adapters; `desktop/flutter` owns the Windows runner and desktop adapters.
Shared Flutter presentation belongs in `packages/torchat-flutter-ui`.
