# Desktop platform adapter

Windows, Linux, and macOS lifecycle, notifications, window management,
autostart, and desktop-only shell integration belong here. Shared UI must use
the platform/runtime contract rather than importing native bridges directly.
