# Android application

`flutter/` is the Android runner and owns Android-specific adapters. Shared UI,
models, localization, and responsive desktop presentation primitives are
owned by `packages/torchat-flutter-ui`.
